# da-kine-repo

## Running the site locally

This is a small Next.js site. You need Node.js 18.18 or newer.

```bash
npm install
npm run dev
```

Then open http://localhost:3000. To make a production build, run `npm run build` followed by `npm start`.
